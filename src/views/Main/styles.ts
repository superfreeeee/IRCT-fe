import styled from 'styled-components';

export const IM_WIDTH = 266;
export const ROOM_SPACE_WIDTH_ROOM = 400;
export const ROOM_SPACE_WIDTH_CHAT = 550;
export const VIDEO_ROOM_WIDTH = 800;

export const Container = styled.div`
  display: flex;
  align-items: flex-start;
  width: 100vw;
  height: 100vh;
  border-radius: 30px;
  background-color: var(--root_bg);
`;
